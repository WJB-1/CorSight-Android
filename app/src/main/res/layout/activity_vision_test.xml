<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 相机预览 -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:scaleType="fitStart" />

    <!-- 网络流预览 -->
    <ImageView
        android:id="@+id/ivNetwork"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <!-- 检测框叠加层 -->
    <com.example.voicenavigation.DetectionOverlayView
        android:id="@+id/overlayView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 顶部：源切换 + 网络配置 -->
    <LinearLayout
        android:id="@+id/topControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#CC000000"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- 源切换按钮 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">

            <Button
                android:id="@+id/btnSourceCamera"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:text="\xF0\x9F\x93\xB7 相机"
                android:textSize="13sp"
                android:textColor="@color/white"
                android:background="@color/purple_700"
                android:layout_marginEnd="4dp" />

            <Button
                android:id="@+id/btnSourceNetwork"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:text="\xF0\x9F\x8C\x90 网络流"
                android:textSize="13sp"
                android:textColor="@color/white"
                android:background="@color/gray"
                android:layout_marginStart="4dp" />
        </LinearLayout>

        <!-- 网络配置区域（默认隐藏） -->
        <LinearLayout
            android:id="@+id/layoutNetworkConfig"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:visibility="gone"
            android:layout_marginTop="6dp">

            <EditText
                android:id="@+id/etStreamIp"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:hint="IP:端口 (如 192.168.1.100:8080)"
                android:singleLine="true"
                android:paddingHorizontal="10dp"
                android:background="@drawable/edit_text_background"
                android:textColor="@color/white"
                android:textColorHint="@color/gray"
                android:textSize="13sp"
                android:text="192.168.1.100" />

            <Button
                android:id="@+id/btnConnectStream"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:text="连接"
                android:textSize="13sp"
                android:textColor="@color/white"
                android:background="@color/teal_200"
                android:paddingHorizontal="16dp"
                android:layout_marginStart="6dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- 底部：检测结果 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#CC000000"
        android:padding="12dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tvDetections"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="初始化中..."
            android:textColor="@android:color/white"
            android:textSize="15sp"
            android:lineSpacingMultiplier="1.3" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
